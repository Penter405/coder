we have done some logic:
1.project process. but not sure if two origin project. will they see other coped project of origin project?
2.prompt to LLM.
there are some logic not finished:
1.from LLM return, let developer contorl aspect or reject LLM return.
2. make sure "difference" toggle switch at enter window will update our new language(it will show difference) or difference code into chat.txt with "Generate" button.